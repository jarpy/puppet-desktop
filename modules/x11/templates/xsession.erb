mate-settings-daemon &      # handles themes, starts gnome-screensaver. You may have to use gconf to disable it setting the background.
glipper &
nm-applet &
exec awesome                 # awesome receives xinit's process id; when it terminates, X will be terminated
